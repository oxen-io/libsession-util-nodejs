{
  "main": "index.js",
  "name": "libsession_util_nodejs",
  "description": "Wrappers for the Session Util Library",
  "version": "0.1.0",
  "license": "GPL-3.0",
  "author": {
    "name": "Oxen Project",
    "email": "team@oxen.io"
  },
  "scripts": {
    "preinstall": "if [ -d libsession-util ]; then (cd libsession-util && git pull && git submodule update --init --recursive); else git clone --recurse-submodules https://github.com/oxen-io/libsession-util.git libsession-util; fi",
    "install": "cmake-js compile"
  },
  "devDependencies": {
    "clang-format": "^1.8.0"
  },
  "dependencies": {
    "cmake-js": "^7.2.1",
    "node-addon-api": "^6.1.0"
  },
  "typings": "index.d.ts"
}
